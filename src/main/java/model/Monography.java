package model;

public class Monography extends Publication{
    public Monography(String title, String author, String publisher, String links){
        super(title, author, publisher, links);
    }
}
